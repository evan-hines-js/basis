# Per-deployment state generated by the playbook. The CA private key
# lives here — never commit it.
pki/
inventory.ini

# Build / cache dirs left behind by older roles (e.g. the cargo build
# of holod when basis used Holo as its BGP daemon). Kept ignored so
# stale dirs from past playbook runs don't pollute git status.
build/
